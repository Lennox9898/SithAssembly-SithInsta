FROM python:3.13-slim

WORKDIR /app
COPY . /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

EXPOSE 8080

# Compatibility container only. Replace this command with FastAPI/Uvicorn after the API migration.
CMD ["python", "app.py", "--host", "0.0.0.0", "--port", "8080"]
