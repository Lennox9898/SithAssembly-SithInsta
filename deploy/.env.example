# Local Docker bind mounts, resolved relative to deploy/.
SITH_HOST_DATA_DIR=../data
SITH_HOST_RUNTIME_DIR=../.runtime
SITH_API_PORT=8080

# Pin image digests in deploy/.env.local before enabling infrastructure profiles.
SITH_POSTGRES_IMAGE=
SITH_NATS_IMAGE=
SITH_STORAGE_IMAGE=
